services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: hireflow
      POSTGRES_PASSWORD: hireflow
      POSTGRES_DB: hireflow
    ports:
      - "5432:5432"
    volumes:
      - hireflow_pg:/var/lib/postgresql/data
volumes:
  hireflow_pg:
